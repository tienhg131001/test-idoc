import CreateDistrict from "../../components/Create/CreateDistrict";

type Props = {};

const index = ({}: Props) => {
  return (
    <div className="w-screen h-screen flex justify-center items-center">
      <CreateDistrict />
    </div>
  );
};

export default index;
